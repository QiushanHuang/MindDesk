name: Release

on:
  workflow_dispatch:
    inputs:
      publish_github_release:
        description: "Create a draft GitHub Release with the packaged artifacts"
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  package:
    name: Developer ID notarized macOS package
    runs-on: macos-15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Validate release ref
        env:
          PUBLISH_GITHUB_RELEASE: ${{ inputs.publish_github_release }}
        run: |
          set -euo pipefail
          VERSION="$(tr -d '[:space:]' <VERSION)"
          if [[ "$GITHUB_REF_TYPE" != "tag" && "$GITHUB_REF_NAME" != "main" ]]; then
            echo "::error::Release workflow must run from main or from a pushed version tag."
            exit 1
          fi
          if [[ "$GITHUB_REF_TYPE" == "tag" && "$GITHUB_REF_NAME" != "v${VERSION}" ]]; then
            echo "::error::Release tag $GITHUB_REF_NAME does not match VERSION v${VERSION}."
            exit 1
          fi
          if [[ "$PUBLISH_GITHUB_RELEASE" == "true" && "$GITHUB_REF_TYPE" != "tag" ]]; then
            echo "::error::Draft GitHub Release publishing must run from the pushed v${VERSION} tag."
            exit 1
          fi

      - name: Set release artifact suffix
        run: |
          set -euo pipefail
          ARCH="$(printf '%s' "${RUNNER_ARCH:-unknown}" | tr '[:upper:]' '[:lower:]')"
          echo "RELEASE_PLATFORM_SUFFIX=macOS-${ARCH}" >>"$GITHUB_ENV"

      - name: Verify release inputs before signing
        run: |
          set -euo pipefail
          VERSION="$(tr -d '[:space:]' <VERSION)"
          test -f "docs/releases/v${VERSION}.md"
          swift test --scratch-path /tmp/minddesk-release-tests
          bash -n script/build_and_run.sh
          bash -n script/package_release.sh
          bash -n script/verify_release_metadata.sh
          ./script/verify_release_metadata.sh
          plutil -lint script/release.entitlements
          git diff --check

      - name: Validate signing and notarization secrets
        env:
          CERTIFICATE_BASE64: ${{ secrets.DEVELOPER_ID_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.DEVELOPER_ID_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.SIGNING_KEYCHAIN_PASSWORD }}
          CODESIGN_IDENTITY: ${{ secrets.DEVELOPER_ID_IDENTITY }}
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
          API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          set -euo pipefail

          require_secret() {
            local name="$1"
            local value="$2"
            if [[ -z "$value" ]]; then
              echo "::error::$name is missing. Configure this repository secret before running the Release workflow."
              exit 1
            fi
          }

          require_secret "DEVELOPER_ID_CERTIFICATE_BASE64" "$CERTIFICATE_BASE64"
          require_secret "DEVELOPER_ID_CERTIFICATE_PASSWORD" "$CERTIFICATE_PASSWORD"
          require_secret "SIGNING_KEYCHAIN_PASSWORD" "$KEYCHAIN_PASSWORD"
          require_secret "DEVELOPER_ID_IDENTITY" "$CODESIGN_IDENTITY"
          require_secret "APPLE_TEAM_ID" "$TEAM_ID"
          require_secret "APP_STORE_CONNECT_API_KEY_BASE64" "$API_KEY_BASE64"
          require_secret "APP_STORE_CONNECT_KEY_ID" "$API_KEY_ID"
          require_secret "APP_STORE_CONNECT_ISSUER_ID" "$API_ISSUER_ID"

          python3 - <<'PY'
          import base64
          import os
          import sys

          checks = [
              ("DEVELOPER_ID_CERTIFICATE_BASE64", os.environ["CERTIFICATE_BASE64"]),
              ("APP_STORE_CONNECT_API_KEY_BASE64", os.environ["API_KEY_BASE64"]),
          ]

          for name, encoded in checks:
              try:
                  base64.b64decode(encoded, validate=True)
              except Exception as exc:
                  print(f"::error::{name} is not valid base64 data: {exc}")
                  sys.exit(1)
          PY

      - name: Import Developer ID certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.DEVELOPER_ID_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.DEVELOPER_ID_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.SIGNING_KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          CERTIFICATE_PATH="$RUNNER_TEMP/developer-id-application.p12"
          KEYCHAIN_PATH="$RUNNER_TEMP/minddesk-signing.keychain-db"
          export CERTIFICATE_PATH

          python3 - <<'PY'
          import base64
          import os
          from pathlib import Path

          encoded = os.environ["CERTIFICATE_BASE64"]
          Path(os.environ["CERTIFICATE_PATH"]).write_bytes(base64.b64decode(encoded))
          PY

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$CERTIFICATE_PATH" \
            -P "$CERTIFICATE_PASSWORD" \
            -T /usr/bin/codesign \
            -T /usr/bin/security \
            -t cert \
            -f pkcs12 \
            -k "$KEYCHAIN_PATH"
          security list-keychains -d user -s "$KEYCHAIN_PATH" $(security list-keychains -d user | sed 's/[ "]//g')
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security find-identity -v -p codesigning "$KEYCHAIN_PATH"

      - name: Decode App Store Connect API key
        env:
          API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          set -euo pipefail
          API_KEY_PATH="$RUNNER_TEMP/AuthKey_${{ secrets.APP_STORE_CONNECT_KEY_ID }}.p8"
          export API_KEY_PATH
          python3 - <<'PY'
          import base64
          import os
          from pathlib import Path

          encoded = os.environ["API_KEY_BASE64"]
          Path(os.environ["API_KEY_PATH"]).write_bytes(base64.b64decode(encoded))
          PY
          chmod 600 "$API_KEY_PATH"
          echo "API_KEY_PATH=$API_KEY_PATH" >>"$GITHUB_ENV"

      - name: Build, sign, notarize, and staple release artifacts
        env:
          CODESIGN_IDENTITY: ${{ secrets.DEVELOPER_ID_IDENTITY }}
          TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          NOTARY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          NOTARY_ISSUER: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
        run: |
          set -euo pipefail
          : "${API_KEY_PATH:?Missing decoded App Store Connect API key path}"
          ./script/package_release.sh \
            --mode notarized \
            --identity "$CODESIGN_IDENTITY" \
            --team-id "$TEAM_ID" \
            --notary-key "$API_KEY_PATH" \
            --notary-key-id "$NOTARY_KEY_ID" \
            --notary-issuer "$NOTARY_ISSUER"

      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: minddesk-macos-${{ runner.arch }}-release
          path: |
            dist/release/*/artifacts/*.zip
            dist/release/*/artifacts/*.dmg
            dist/release/*/artifacts/*.txt
            dist/release/*/artifacts/*.md
            dist/release/*/artifacts/*.json
            dist/release/*/artifacts/*.plist

      - name: Upload release diagnostics
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: minddesk-macos-${{ runner.arch }}-release-diagnostics
          if-no-files-found: ignore
          path: |
            dist/release/*/artifacts/notary-*.json
            dist/release/*/artifacts/notary-*.stderr
            dist/release/*/artifacts/codesign-*.txt
            dist/release/*/artifacts/codesign-*.plist

      - name: Create draft GitHub Release
        if: ${{ inputs.publish_github_release }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          VERSION="$(tr -d '[:space:]' <VERSION)"
          ARTIFACT_DIR="dist/release/MindDesk-v${VERSION}-${RELEASE_PLATFORM_SUFFIX}/artifacts"
          if gh release view "v${VERSION}" >/dev/null 2>&1; then
            gh release edit "v${VERSION}" \
              --draft=true \
              --title "MindDesk v${VERSION}" \
              --notes-file "$ARTIFACT_DIR/RELEASE-NOTES.md"
            gh release upload "v${VERSION}" \
              "$ARTIFACT_DIR/MindDesk-v${VERSION}-${RELEASE_PLATFORM_SUFFIX}.zip" \
              "$ARTIFACT_DIR/MindDesk-v${VERSION}-${RELEASE_PLATFORM_SUFFIX}.dmg" \
              "$ARTIFACT_DIR/SHA256SUMS.txt" \
              "$ARTIFACT_DIR/INSTALL.txt" \
              "$ARTIFACT_DIR/RELEASE-NOTES.md" \
              --clobber
          else
            gh release create "v${VERSION}" \
              "$ARTIFACT_DIR/MindDesk-v${VERSION}-${RELEASE_PLATFORM_SUFFIX}.zip" \
              "$ARTIFACT_DIR/MindDesk-v${VERSION}-${RELEASE_PLATFORM_SUFFIX}.dmg" \
              "$ARTIFACT_DIR/SHA256SUMS.txt" \
              "$ARTIFACT_DIR/INSTALL.txt" \
              "$ARTIFACT_DIR/RELEASE-NOTES.md" \
              --draft \
              --verify-tag \
              --title "MindDesk v${VERSION}" \
              --notes-file "$ARTIFACT_DIR/RELEASE-NOTES.md"
          fi
